const BOOKING_STATUS = ["In Process", "Received", "Cancel"];

const BOL_STATUS = ["Not Ready", "Ready", "In Process", "Received", "Cancel"];

const INVOICE_STATUS = ["Not Ready", "Received", "Cancel"];

module.exports = { BOOKING_STATUS, BOL_STATUS, INVOICE_STATUS }